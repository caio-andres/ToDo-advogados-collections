info:
  name: criar usuario
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}usuarios"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "nome": "Caio André34323",
        "email": "caio@emailsasaaaa.com",
        "senha": "Aa12344321"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const jsonData = res.getStatus()

        if (res.getStatus() === 200 || res.getStatus() === 201) {
            bru.setEnvVar("user_id", res.getBody().data.id);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

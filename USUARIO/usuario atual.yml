info:
  name: usuario atual
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}usuarios/me"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

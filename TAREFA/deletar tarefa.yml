info:
  name: deletar tarefa
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}tarefas/{{tarefa_id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

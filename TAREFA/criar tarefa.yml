info:
  name: criar tarefa
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}tarefas"
  body:
    type: json
    data: |-
      {
        "titulo": "ir ao fórum",
        "descricao": "bora la",
        "status": "pendente"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status === 201) {
          bru.setEnvVar("tarefa_id", res.body.data.id);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: listar tarefas
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}tarefas"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

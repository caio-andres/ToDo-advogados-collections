info:
  name: atualizar tarefa
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}tarefas/{{tarefa_id}}"
  body:
    type: json
    data: |-
      {
        "titulo": "Revisar contrato - ATUALIZADO",
        "descricao": "Revisar cláusulas do contrato de prestação de serviços jurídicos - versão final"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: listar concluida
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}tarefas?status=concluida"
  params:
    - name: status
      value: concluida
      type: query
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

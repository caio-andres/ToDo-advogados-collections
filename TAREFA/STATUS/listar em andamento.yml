info:
  name: listar em andamento
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}tarefas?status=em_andamento"
  params:
    - name: status
      value: em_andamento
      type: query
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: listar pendente
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}tarefas?status=pendente"
  params:
    - name: status
      value: pendente
      type: query
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

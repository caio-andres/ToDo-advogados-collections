info:
  name: atualizar status
  type: http
  seq: 6

http:
  method: PUT
  url: "{{url}}tarefas/{{tarefa_id}}"
  body:
    type: json
    data: |-
      {
        "status": "concluida"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
